# Frontend environment variables
# MSAL config is hardcoded in src/config/msalConfig.ts for this SPA.
# API calls use the Vite proxy in dev (/api -> backend).

# Auth mode: "azure" (real MSAL login) or "mock" (skip auth for local testing)
VITE_AUTH_MODE=mock
